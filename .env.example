# 模型渠道（两类：local / cloud）
# local: 本地 Ollama
# cloud: 阿里百炼 DASHSCOPE（OpenAI 兼容协议，未来可替换为其他云端）
LLM_PROVIDER=local
# 可选值：local / dashscope / openai

# 按功能指定模型（不填则使用对应渠道默认模型）
LLM_MODEL_REPORT=
LLM_MODEL_CHAT=
# 五行能量分析默认使用结构化输出能力更强的模型
LLM_MODEL_ENERGY=qwen-plus-2025-12-01
# 大运解析默认使用结构化输出能力更强的模型
LLM_MODEL_DESTINY=qwen-plus-2025-12-01

# 可选：按功能覆盖温度与超时（不填则用渠道默认配置）
LLM_MODEL_REPORT_TEMPERATURE=
LLM_MODEL_REPORT_TIMEOUT=
LLM_MODEL_REPORT_ENABLE_THINKING=
LLM_MODEL_CHAT_TEMPERATURE=
LLM_MODEL_CHAT_TIMEOUT=
LLM_MODEL_CHAT_ENABLE_THINKING=
LLM_MODEL_ENERGY_TEMPERATURE=
LLM_MODEL_ENERGY_TIMEOUT=
LLM_MODEL_ENERGY_ENABLE_THINKING=false
LLM_MODEL_DESTINY_TEMPERATURE=
LLM_MODEL_DESTINY_TIMEOUT=
LLM_MODEL_DESTINY_ENABLE_THINKING=


# =====================
# local: 本地 Ollama
# =====================
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=deepseek-r1:8b
OLLAMA_TEMPERATURE=0.7
OLLAMA_TIMEOUT=60


# =====================
# cloud: 阿里百炼 DASHSCOPE
# =====================

# 获取 Key 后填到这里（不要提交真实 key）
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 默认模型（如 deepseek-v3.2 / qwen-plus-2025-12-01）
DASHSCOPE_MODEL=deepseek-v3.2
DASHSCOPE_TEMPERATURE=0.7
DASHSCOPE_TIMEOUT=60
# 仅对云端生效：默认是否开启思考模式（enable_thinking）
DASHSCOPE_ENABLE_THINKING=true

# =====================
# OpenAI 兼容变量（可选，等价于 DASHSCOPE_*）
# =====================
OPENAI_COMPAT_API_KEY=
OPENAI_COMPAT_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_COMPAT_MODEL=deepseek-v3.2
OPENAI_COMPAT_TEMPERATURE=0.7
OPENAI_COMPAT_TIMEOUT=60
OPENAI_COMPAT_ENABLE_THINKING=true

# 兼容旧变量（保留即可，用于历史配置）
DEEPSEEK_ENABLE_THINKING=true
